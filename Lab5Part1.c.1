#include <stdio.h>
#include <math.h>

//To find the input from the user and save it as a pointer
void input(int *num){
    do{
        
        printf("Please enter an integer greater than 99999: ");
        scanf("%d", num);
        if (*num<99999){
            printf("Incorrect input.\n");
        }
    } while (*num<99999);
    printf("The number entered is %d\n", *num);

}

//To add four to each of the digits in the number
int add4(int num){
    int i = 0;
    int newNum = 0;
    int temp = 0;
    
	//This for loop adds four to each digit
    for (i=0; num!=0;i++){
        temp = (((num%10))+4)%10;
        num = num/10;
        newNum = newNum + temp * pow(10, i);
    }
    
    return newNum;
}
    
    
    // This function is used to shift the digits to the left
    int shift(int num){
        int temp = num;
        int lastDigit = 0;
        int newNum = 0;
        int temp3 = 0;
        int i = 0;
        
		//This determines the length of the number
        int size=0;
        while (temp!=0){
            temp =temp/10;
            size ++;
        }
        
        //This shifts the number 
        do{
            temp3 = (num/pow(10,i));
            temp3 = temp3%10;
            newNum  = newNum + temp3 * pow(10, i+1);
            i++;
        }while (i<size-1);
        
        // This adds the leftmost digit of the number to the right side of the new number
        lastDigit = (num/pow(10,size-1));
        newNum = newNum + lastDigit;
        return newNum;
    }
    

// Print out the final messages
void printOutput (int encryptNum, int originalNum){
    printf("Original number: %d\n", originalNum);
    printf("Encrypted number: %d\n", encryptNum);
}
